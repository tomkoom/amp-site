import React, { FC } from "react"
import { styled } from "styled-components"

const Scan: FC = (): JSX.Element => {
  return <ScanStyled>Scan</ScanStyled>
}

const ScanStyled = styled.div``

export default Scan
